# Local Task Manager

Offline task, event, and note manager with unique IDs (`[Type][Category][Month][Sequence]`), using localStorage for persistence. Add, edit, delete items, and manage deleted items in a **Recycle Bin** with restore or permanent delete—fully offline and minimal.
