# Local Task Manager

Offline task, event, and note manager with unique IDs (`[Type][Category][Month][Sequence]`), using localStorage for persistence. Add, edit, delete, and filter items in your browser—fully offline, minimal, and self-contained.
